import os  # Imports the os module, which provides functions for interacting with the operating system.

print("this is my first image")  # Prints a simple message.
print("current Dir is: ", os.getcwd())  # Displays the current working directory.
print(os.listdir())  # Lists all files and directories in the current directory.

